# SmartFund.Web

React + TypeScript dashboard for the SmartFund API.

## Prerequisites
- Node.js 18+ (recommended 20+)

## Setup
```bash
cd SmartFund.Web
npm install
npm run dev
```

The app is configured to call the backend at `https://localhost:7274/api`.
